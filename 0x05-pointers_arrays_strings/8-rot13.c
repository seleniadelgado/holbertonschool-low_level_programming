#include "holberton.h"

/**
 * *rot13 - Write a function that encodes a string using rot13.
 * @s: parameter used. 
 * Return: s.
 */
char *rot13(char *s)
{
	int a;

	a = 0;
	while (a != '\0')
		a++;
	return(s);
}
